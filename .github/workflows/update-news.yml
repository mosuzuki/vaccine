name: Update dashboard and deploy Pages

on:
  workflow_dispatch:
  schedule:
    - cron: '10 21 * * *'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser requests deep-translator rapidfuzz trafilatura

      - name: Fetch and build data
        run: python scripts/fetch_news.py

      - name: Persist updated data files
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add data/news.json data/archive.json data/translation_cache.json data/article_text_cache.json
          if ! git diff --cached --quiet; then
            git commit -m "Update monitoring data"
            git push
          else
            echo "No data changes to commit"
          fi

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
